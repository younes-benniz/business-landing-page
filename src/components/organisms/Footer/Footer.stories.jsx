import React from "react";
import { Footer } from ".";

export default {
	title: "section",
	component: Footer,
};

const Template = (args) => <Footer {...args} />;

export const footer = Template.bind({});
