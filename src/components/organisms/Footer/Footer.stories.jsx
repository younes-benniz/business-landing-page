import React from "react";
import { Footer } from ".";

export default {
	title: "Organisms",
	component: Footer,
};

const Template = (args) => <Footer {...args} />;

export const footer = Template.bind({});
