import React from "react";
import { StackSection } from ".";

export default {
	title: "Organisms",
	component: StackSection,
};

const Template = (args) => <StackSection {...args} />;

export const Stack = Template.bind({});
