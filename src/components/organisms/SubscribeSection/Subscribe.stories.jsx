import React from "react";
import { SubscribeSection } from ".";

export default {
	title: "Organisms",
	component: SubscribeSection,
};

const Template = (args) => <SubscribeSection {...args} />;

export const Subscribe = Template.bind({});
