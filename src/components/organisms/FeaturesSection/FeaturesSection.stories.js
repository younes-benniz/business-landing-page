import React from "react";
import { FeaturesSection } from ".";

export default {
	title: "Organisms",
	component: FeaturesSection,
};

const Template = (args) => <FeaturesSection {...args} />;

export const Features = Template.bind({});
