import React from "react";
import { SubscribeForm } from ".";

export default {
	title: "Molecules/SubscribeForm",
	component: SubscribeForm,
};

const Template = (args) => <SubscribeForm {...args} />;

export const subscribeForm = Template.bind({});
